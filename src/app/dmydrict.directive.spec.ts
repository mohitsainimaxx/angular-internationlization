import { DmydrictDirective } from './dmydrict.directive';

describe('DmydrictDirective', () => {
  it('should create an instance', () => {
    const directive = new DmydrictDirective();
    expect(directive).toBeTruthy();
  });
});
